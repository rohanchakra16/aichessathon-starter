You are the supervising chess-engine researcher for the AI Chessathon bot
Phineas, performing the mandatory between-batch evidence audit from
docs/autoloop/claude-takeover-prompt.md and
docs/autoloop/claude-handoff-2026-09-03.md.

You have read-only tools (Read, Grep, Glob). The working directory is the repo
root. Use them to inspect anything you need: agent.py (the current champion),
experiments/exp-*.json (full records, including game PGNs, for the batch just
run and any earlier experiment), docs/autoloop/*, research/*, .autoloop/state.json,
.autoloop/protected/policy.json. Do not attempt to edit anything.

## Fixed context

Champion commit: 4a0c988009ecae163ac09368f92e4a792dac7568
Live competition upload: older exp-0052-era champion (do not upload; out of scope).
next_experiment in state.json: 97
Consecutive scientific non-improvements before this audit: 7

## Recent experiment registry (newest first, compact)

- exp-0096: inconclusive; HYPOTHESIS: Adding a bounded (~28-unit max) halfmove-clock progress gradient to `_model_evaluate`, gated to reduced-material (`phase <= 10`) and mater
- exp-0095: inconclusive; HYPOTHESIS: Adding a bounded, gated coordinate-based mop-up term to `_model_evaluate` that only fires in lone-king endgames — rewarding driving the ba
- exp-0094: inconclusive; HYPOTHESIS: Scoring the first in-search-tree repetition of a position as a draw (0.0), rather than waiting for the on-board third occurrence, gives al
- exp-0093: inconclusive; HYPOTHESIS: Adding a cheap flat doubled-pawn (12 cp) and isolated-pawn (14 cp) penalty to `_model_evaluate`, computed from a per-file pawn tally folde
- exp-0092: inconclusive; HYPOTHESIS: Adding a flat 18 cp open-file / 9 cp half-open-file rook bonus to `_model_evaluate` (via two once-per-call pawn bitboards and a per-rook f
- exp-0091: inconclusive; HYPOTHESIS: Adding a cheap precomputed rank-scaled passed-pawn bonus to the leaf evaluation gives alpha-beta a monotone progress gradient in locked an
- exp-0090: inconclusive; HYPOTHESIS: Adding a cheap quadratic king-danger term to `_model_evaluate` — counting enemy-attacked squares of each king's one-step ring (capped at 6
- exp-0089: accepted; HYPOTHESIS: Adding a cheap fixed 3-centipawn-per-square net-mobility differential to the leaf evaluation gives alpha-beta a monotone gradient toward a
- exp-0088: failed; 
- exp-0087: inconclusive; HYPOTHESIS: Replacing per-move UCI-string tie-breaking in move ordering with a cheap packed-integer key removes throwaway string allocation at every n
- exp-0086: inconclusive; HYPOTHESIS: Aspiration-window iterative deepening reduces root search effort while full-window recovery preserves correctness, enabling deeper searche
- exp-0085: inconclusive; HYPOTHESIS: Per-ply killer-move ordering surfaces empirically strong quiet refutations earlier in alpha-beta, yielding more cutoffs and greater search
- exp-0084: inconclusive; HYPOTHESIS: Depth-preferred transposition-table principal-move reuse improves alpha-beta cutoffs and search depth by searching previously successful m
- exp-0083: rejected; HYPOTHESIS: Prioritizing forcing checks in move ordering improves alpha-beta cutoffs and search depth under the fixed clock without changing evaluatio
- exp-0082: inconclusive; 
- exp-0081: inconclusive; HYPOTHESIS: Depth-aware mate scores prioritize faster forced wins and delay unavoidable losses, improving decisive-line selection without altering lea
- exp-0080: inconclusive; HYPOTHESIS: Exception-safe move unwinding in negamax and quiescence prevents deadline timeouts from corrupting board state, improving reliable timeout
- exp-0079: inconclusive; HYPOTHESIS: Adding zugzwang-guarded null-move pruning lets iterative deepening reach one to two plies deeper within the same clock budget, sharpening 
- exp-0078: inconclusive; HYPOTHESIS: A capped final check-evasion ply prevents static evaluation of unresolved checks, reducing horizon errors without allowing unbounded quies
- exp-0077: inconclusive; HYPOTHESIS: Including non-capturing promotions in quiescence will prevent severe horizon misvaluations of advanced pawns and improve tactical move sel

## New experiment record(s) from the batch just run

{
  "id": "exp-0096",
  "status": "inconclusive",
  "generator": "claude-code",
  "hypothesis": "HYPOTHESIS: Adding a bounded (~28-unit max) halfmove-clock progress gradient to `_model_evaluate`, gated to reduced-material (`phase <= 10`) and materially decided (`|material_diff| >= 300`) endgames, that penalises the side ahead and rewards the side behind as the 50-move counter climbs, gives alpha-beta a monotone tie-break toward clock-resetting pawn moves, captures and trades \u2014 cutting the non-forced threefold-repetition draws from winning positions and steering lost endgames toward the 50-move draw instead of long king hunts.",
  "decision_reason": "sequential paired arena remained inconclusive (score 0.539, interval [0.467, 0.610])",
  "consecutive_non_improvements": 6,
  "candidate_commit": "c56e3682421c4e04e0883cd4cd4aa5ec3a72bb0d",
  "arena": {
    "wins": 22,
    "draws": 25,
    "losses": 17,
    "score": 0.5390625,
    "statistical_decision": "inconclusive",
    "confidence_interval": {
      "lower": 0.46653257783735813,
      "upper": 0.6099752712999822
    },
    "agent_failures": 0
  },
  "ci_passed": true,
  "reliability_stress_passed": true,
  "draw_terminations": {
    "threefold_repetition": 25
  },
  "loss_breakdown": [
    {
      "colour": "black",
      "termination": "checkmate"
    },
    {
      "colour": "black",
      "termination": "checkmate"
    },
    {
      "colour": "black",
      "termination": "checkmate"
    },
    {
      "colour": "white",
      "termination": "checkmate"
    },
    {
      "colour": "black",
      "termination": "checkmate"
    },
    {
      "colour": "black",
      "termination": "checkmate"
    },
    {
      "colour": "black",
      "termination": "checkmate"
    },
    {
      "colour": "black",
      "termination": "checkmate"
    },
    {
      "colour": "white",
      "termination": "checkmate"
    },
    {
      "colour": "white",
      "termination": "checkmate"
    },
    {
      "colour": "white",
      "termination": "checkmate"
    },
    {
      "colour": "white",
      "termination": "checkmate"
    },
    {
      "colour": "white",
      "termination": "checkmate"
    },
    {
      "colour": "black",
      "termination": "checkmate"
    },
    {
      "colour": "black",
      "termination": "checkmate"
    },
    {
      "colour": "black",
      "termination": "checkmate"
    },
    {
      "colour": "white",
      "termination": "checkmate"
    }
  ],
  "record_path": "experiments/exp-0096.json"
}

Controller stdout (tail):
exp-0096: inconclusive: sequential paired arena remained inconclusive (score 0.539, interval [0.467, 0.610])


## Mechanism families already recorded as exhausted in their tested form

- history / killer / TT-best-move move ordering
- aspiration windows
- guarded null-move pruning
- bounded check extensions / evasions
- general forcing-check priority (decisively worse)
- mate-distance scoring
- quiet promotions in quiescence
- quiescence delta pruning
- draw contempt
- halfmove-clock TT bucketing / early repetition-call guarding
- timeout exception unwinding
- exact quiescence stalemate repair
- move-ordering key micro-optimisation (packed int vs UCI string)
- time-budget / clock allocation changes without a structural search change
- simple six-feature linear king-safety residual (offline: no signal)

Changing only a cap, threshold, depth, margin, table size, ordering weight or
time constant of any of the above is NOT a materially new hypothesis.

## Previous audit

{
  "completed_at": "2026-09-03T23:44:44.231863+00:00",
  "after_experiment": "exp-0095",
  "decision": "CONTINUE",
  "next_direction": {
    "title": "Halfmove-clock progress gradient in the leaf evaluation for reduced-material endgames",
    "family": "leaf-evaluation-progress-gradient",
    "hypothesis": "In agent.py's _model_evaluate, fold a cheap side-relative non-king material total into the existing piece loop (accumulate material_diff += sign * SEE_VALUES[piece_type] * len(squares) for piece_type != chess.KING, reusing the squares list and sign already computed). After the midgame/endgame blend and the existing mobility/castling terms, apply a bounded progress gradient gated to reduced-material, materially decided endgames: if phase <= 10 (both sides reduced to roughly a queen, or a rook plus a minor, or less) AND abs(material_diff) >= 300 (at least a clean minor-piece advantage for one side), compute pull = min(board.halfmove_clock, 80) * 0.35 and do score -= math.copysign(pull, material_diff). This penalises the side that is ahead for a rising 50-move counter (maximum about 28 model units, below a pawn and far below any mate score, so it can only break ties and can never invert a position material already calls won or lost), giving alpha-beta a monotone reason to prefer clock-resetting pawn moves, captures and trades over the king/piece shuffling that currently produces 32 of 33 draws by threefold repetition; and symmetrically it rewards the side that is behind for a rising counter, giving the engine a reason to steer lost endgames toward the 50-move draw rather than into the long king hunts that account for all 18 decisive losses. No change to _ordered_moves, _quiescence, _negamax, the transposition-table key or size, _forced_draw, _budget_seconds, the iterative-deepening loop, MATE handling, or weights/.",
    "rationale": "The dominant retained failure across six straight experiments is non-forced threefold-repetition draws, many from winning or equal positions, plus a loss column that is entirely checkmates after passive simplification. The mechanical cause is specific and untouched: the trained piece-square model has no move-counter feature, its PST is identical for a king or piece shuffled between two squares, and the accepted mobility term (exp-0089) is near zero for a lone or cramped king, so the leaf evaluation cannot distinguish steady progress from aimless shuffling in a technically winning position, and a missing gradient cannot be recovered by searching deeper because the converting mate is 15-25 plies past the horizon. A halfmove-clock term is the minimal closed-form way to inject that gradient. It is materially different from every exhausted mechanism: draw contempt (exp-0063) only re-values an already-recognised draw and supplies no direction toward the win; in-tree repetition scoring (exp-0094) changes a search-tree value and needs the repetition already on the board or in the line; mate-distance scoring acts only once a mate is found in-tree; and exp-0095's coordinate mop-up term was gated to bare-king endings that fired in essentially none of the 64 arena games, so it left endgame-conversion evaluation genuinely untested rather than refuted. A clock gradient fires in every reduced-material endgame regardless of piece configuration (queen, rook, rook-versus-minor, king-and-pawn races) and directly rewards the concrete progress moves the engine currently declines. It keeps the trained model fully determinative of every middlegame and every non-reduced endgame, and of every model-move-ablation opening, and it is a roughly ten-line self-contained change with four hand-chosen scalars, no coefficient from training and no weights change.",
    "guardrails": "Scope: edit only agent.py; weights/ untouched; no change to _ordered_moves, _quiescence, _negamax, the TT key or size, _forced_draw, _budget_seconds, or the iterative-deepening loop. NPS / leaf cost: the material accumulator must be a single multiply-add folded into the existing board.pieces loop, and the post-loop block is about four integer comparisons plus one math.copysign when the gate passes and near-nothing when it fails (virtually every middlegame node and every model-ablation node); target node cost within 1.02x and confirm the paired arena reaches search depth comparable to exp-0095. Correctness bound: hard-cap the contribution at min(halfmove_clock, 80) * 0.35, about 28 model units, so it can only break ties among equal-material shuffle positions and can never flip the sign of a position material evaluation already calls winning or losing; require abs(material_diff) >= 300 so the term never fires in materially level endings or in single-pawn endings such as K+P versus K or R+P versus R where rushing to reset the clock is often the wrong idea and a draw may be correct; require phase <= 10 so the term is completely inert in the middlegame. Model-move-ablation safety: the 3 s ablation openings start from standard opening positions and effectively never reach a phase <= 10, >= 300 cp endgame, so the term is inert on that path; require model_move_ablation to still pass with minimum_differences >= 8 and the observed differences materially unchanged from exp-0095's 28 of 32, and confirm the arena strength gate is not regressed. Reliability / flag risk: no new hot-path work, so re-verify the full stress_time_left_ms grid (1 / 25 / 100 / 1000 / 120000 ms) with required_failures 0 and init under 50 s; exp-0095's worst single move was 0.87 s against the 2.25 s ceiling and 0.0002 s against 0.75 s at the 1 ms clock, and this change adds no measurable work, so that margin must be preserved. CI, ruff, mypy, smoke, resource and policy gates must stay green with zero agent failures. Anti-tuning: if the paired arena is inconclusive, do NOT re-run this by adjusting the 0.35, 80, 300 or phase-cap constants, by making the term asymmetric, by switching from halfmove_clock to a ply count, or by adding a second clock-derived term and calling it a new experiment; that outcome is the seventh consecutive non-improvement and the next audit should declare stop_condition scientific_saturation and hand the human an offline retrain of weights/model.json with trained positional and endgame features (king activity, pawn structure, passed pawns, and a tempo/progress signal), since every self-contained hand-weighted leaf option will then be exhausted."
  },
  "audit_summary": "Champion is agent.py at commit 4a0c988 (recorded as exp-0089): PVS/LMR negamax with exchange-aware capture ordering and losing-capture pruning in a depth-3 quiescence, a depth-keyed transposition table on (transposition_key, halfmove_clock, depth), partial root-iteration salvage, and a fast draw path (_forced_draw = halfmove_clock>=100 OR is_repetition(3) OR insufficient material), over a tapered learned piece-square model plus an accepted fixed 3 cp/square net-mobility leaf term and a flat castling-rights scalar. The batch just completed produced exp-0095, the previous audit's chosen direction: a bounded coordinate-based mop-up king-drive term added to _model_evaluate, gated strictly to won lone-king endgames and hard-capped at 60 units. It returned an inconclusive sequential paired arena (13 wins, 33 draws, 18 losses, score 0.461, CI [0.390, 0.533]) with CI, model-move ablation (28 of 32 move differences, minimum 8), reliability stress (init 0.22 s, worst move 0.87 s vs the 2.25 s ceiling), resources (peak 120 MB vs 2 GB), ruff, mypy, smoke and policy gates all green and zero agent failures; not promoted, so the mop-up term is not in the champion. This is the sixth consecutive scientific non-improvement since exp-0089 (exp-0090 0.508, exp-0091 0.531, exp-0092 0.492, exp-0093 0.484, exp-0094 0.531, exp-0095 0.461), so the numeric half of the stop rule is met and the search/ordering/quiescence/time family and the middlegame positional-scalar family are both saturated. exp-0095's retained games show the failure modes unchanged: 32 of 64 draws by threefold repetition, many from winning or equal positions with aimless shuffling, whole opening pairs (open-game, english, reti, caro-kann, kings-indian, pirc, dutch, nimzo-indian, slav, french) drawn by both colours; and all 18 losses are checkmates by long king hunts after passive simplification, skewed toward the candidate as White. Critically, exp-0095's gate (one side has mating material, the other is a bare king) fired in essentially none of the 64 games (1 insufficient-material draw versus 32 repetition draws that still had pieces on both sides), so its neutral result reflects an untriggered mechanism rather than a refuted hypothesis. Decision: CONTINUE, stop_condition null, because one materially different, well-motivated, self-contained experiment remains that no record in the window has tried and that targets the dominant failure at its mechanical cause: a bounded halfmove-clock progress gradient in _model_evaluate, gated to reduced-material endgames (phase <= 10) with a clean material edge (>= 300 cp), that penalises the winning side for a rising 50-move counter and symmetrically rewards the losing side, hard-capped near 28 units, well below a pawn. The trained piece-square model has no move-counter feature and identical PST for shuffled positions, so the shuffle-versus-progress distinction is invisible to the leaf evaluation and unreachable by deeper search; this term is the minimal closed-form fix. It is distinct from draw contempt (exp-0063, which only re-values a recognised draw and gives no direction), from in-tree repetition scoring (exp-0094, a search-value change that needs the repetition already present), from mate-distance scoring (acts only after a mate is found in-tree), and from the exp-0095 mop-up term (lone-king only). It keeps the trained model determinative of every middlegame and every non-reduced endgame and every ablation opening, adds negligible node cost behind a gate that piggybacks on the existing piece loop, and touches no protected surface, no weights, no time management and no TT structure. This is the last self-contained evaluation idea: if exp-0096 is inconclusive the next audit should declare scientific_saturation and escalate an offline positional/endgame-feature retrain of weights/model.json to the human. Uploading remains out of scope; the live submission is v3 (artifact sha prefix 55fec84c6dab, state.json status active) tied to the exp-0089 champion, and this audit does not touch it."
}

## Your task

1. Inspect the champion, the new record(s), and whatever retained
   losing/drawn-game evidence or profiling you need. Note recurring failure
   modes (e.g. threefold-repetition draws in balanced or better positions,
   endgame conversion, king-attack defence).
2. Judge whether the current search/move-ordering/quiescence/time-management
   tuning direction is saturated per the handoff's stop rule (>= 5 consecutive
   scientific non-improvements since the newest promotion AND no materially
   different, well-motivated experiment left in that direction).
3. Decide CONTINUE or STOP.
   - CONTINUE if a materially different, well-motivated experiment remains,
     whether in that direction or another (richer leaf evaluation such as
     mobility / pawn structure / passed pawns / bishop pair / rook-on-open-file
     / non-linear king safety; a small opening book designed to not defeat the
     model-move-ablation gate; persisted cross-move search state; something you
     identify from the evidence). Prefer genuinely new directions over more
     tuning.
   - STOP only for: a genuine infrastructure/auth/rules blocker
     (stop_condition "infrastructure_blocker"); or true scientific saturation
     (stop_condition "scientific_saturation") after a full evidence audit with
     no materially new well-motivated experiment anywhere.
4. If CONTINUE, specify the single next hypothesis. Constraints on it:
   - The candidate generator is one `claude -p` run restricted to editing
     agent.py and/or weights/. It has no Bash and cannot run training/ pipelines
     or produce a newly trained weights/model.json. So the hypothesis must be
     self-contained: a code change in agent.py, optionally with small
     hand-chosen or closed-form coefficients, or a change to how existing
     weights are used. If a direction truly needs an offline-trained artifact,
     say so explicitly and scope a self-contained first step the candidate can
     ship now (e.g. a single cheaply-computed feature with a conservative fixed
     coefficient) and flag the training follow-up for the human.
   - It must keep the trained model materially determining leaf evaluation and
     move selection, hold one CPU / 2 GB / no network, keep get_move returning a
     legal UCI move under the real clock, and not repeat a saturated family.
   - Give concrete guardrails (NPS budget, model-ablation-gate safety,
     reliability/flag risk).

## Required structured output

Return exactly one object matching the JSON Schema enforced by the Claude CLI.
Do not wrap it in Markdown, prose, code fences or XML tags. Put the important
analysis in `streak_assessment`, `recurring_failure_modes`, `rationale` and
`audit_summary`. For CONTINUE, `stop_condition` must be null and
`next_direction` must be a complete object. For STOP, `stop_condition` must be
`infrastructure_blocker` or `scientific_saturation`, and `next_direction` may
be null.